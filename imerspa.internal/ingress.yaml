apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: imerspa-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - imerspa.internal
    secretName: tls-certificate
  rules:
  - host: imerspa.internal
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: imerspa-service
            port:
              number: 80

